# Installation

Compatibility entry. Current setup instructions are in
[Getting Started](/guide/getting-started).

Useful commands:

```bash
pnpm install
pnpm dev
pnpm electron:dev
pnpm build:desktop
pnpm docs:build
```
