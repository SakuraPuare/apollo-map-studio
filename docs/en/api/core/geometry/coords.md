# Core / geometry / coords

Source: `src/core/geometry/coords.ts`.

Small conversion helpers between editor `GeoPoint` records and `[lng, lat]`
tuples. Used by draw commit, interpolation and entity mutation paths.

For broader geometry context, see [Geometry Engine](/en/architecture/geometry-engine).
