# Core / action-registry

This page is a compatibility entry. The canonical reference is
[Actions Registry](/en/api/core/actions-registry).

Use `src/core/actions/registry/definitions.ts` for action definitions and
`src/hooks/useActionDispatcher.ts` for side effects.
